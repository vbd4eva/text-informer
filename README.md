# text-informer 
======================
Алгоритм:
================
-> 1. Отримує вхідні данні: (із "HTML інтерфейсу")
    1.1 "вхідну строку"
    1.2 "Ім'я функціі обробки строки"
-> 2. Обробляє вхідні данні (валідує данні та передає їх у метод )
    2.1- отримує функцію за її їм'ям або FALSE 
    2.2- отримує строку або FALSE якщо строка пуста 
    2.3- якщо "вхідну строку" та "функціі обробки строки" отримано переходить до наступного кроку
-> 3. Застосовує на "вхідну строку" "функцію обробки строки" , додає об'єкт "поточного результату" до масиву результатів.
-> 4. Обробляє "массив виконаних Дій" і передає у ("HTML інтерфейс")

Етапи розробки:
================
-> Основний алгоритм
    - функція запуску алгоритму і отримання ("вхідної строки", "переліку Імен функціі обробки строк") (із "HTML інтерфейсу")
    - валідація строк
    - отримання функцій по їх іменах
    - застосування до "вхідної строки" "функцій обробки"
    - додавання  поточних резутьтатів обробки до "масиву результатів"
    - запуск функції обробки "масиву результатів" та передачі даних у ("HTML інтерфейс")

-> Функції обробки тексту
    - описати самі функції
    - метод отримання функції за її ім'ям
    - метод отримання переліку імен наявних функції

-> HTML інтерфейс
    - функція передачі "вхідної строки" та "переліку імен функціі обробки строки" у "Алгоритм"
      та запуск старту "Алгоритма"
    - визначення механізму взаємодії ("HTML інтерфейсу") з "Алгоритмом"        
        - отримання данних зі сторінки:  "вхідна строка", "перелік імен функціі обробки строки"
        - трігер запуску старту алгоритму
        - передача данних на сторінку
            - "перелік наявних імен функціі обробки строки" 
            - передача результатів

-> Оформлення СSS

-> Перенесення строкових данних у Обїєкт


Функції обробки тексту
===============

1. підраховуватиме кількість унікальних слів у тексті.
    алгоритм:
      1. Бере перше слово тексту 
      2. Записує {слово:1}
    3. діле split(слово)
    4. по длинне получившегося массива отределяет сколько таких слов было в тексте
    5. по довжині отримагного масиву визначає кількість таких слів у тексті
    6. до отримагного масиву застосовується join()
    7. повторює пункт #1
--
2. підраховуватиме кількість літер у тексті.


